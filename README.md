# Статистика МПГУ

Веб-приложение для обработки и отображения статистики данных МПГУ, ЦИН и ЕГЛ. Приложение позволяет загружать Excel файлы с дневной и часовой статистикой, обрабатывать данные и отображать их в удобном табличном формате.

## Основные возможности

- **Загрузка файлов**: Поддержка Excel файлов (.xlsx, .xls) для дневной и часовой статистики
- **Обработка данных**: Автоматическое извлечение данных из указанных столбцов Excel файлов
- **Визуализация**: Отображение данных в таблице с условным форматированием
- **Расчеты**: Автоматический расчет производных метрик (проценты, средние значения)
- **Экспорт**: Возможность экспорта таблицы в Excel и копирования как изображение
- **Архив**: Сохранение обработанных таблиц в архив с возможностью просмотра и экспорта
- **Авторизация**: JWT авторизация через Firebase Authentication
- **Логирование действий**: Система логирования всех действий пользователей с понятными описаниями
- **Администрирование**: Управление пользователями и просмотр логов для администраторов

## Технические детали

### Используемые технологии

- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Архитектура**: Модульная структура с разделением ответственности (Separation of Concerns)
- **Библиотеки**:
  - `xlsx.full.min.js` - для работы с Excel файлами
  - `html2canvas` - для конвертации таблицы в изображение
  - Firebase SDK - для аутентификации и базы данных
- **Хостинг**: Render (Static Site)
- **База данных**: Firebase Realtime Database
- **Аутентификация**: Firebase Authentication

## Архитектура

Приложение использует модульную архитектуру с разделением ответственности:

- **Services** - бизнес-логика и работа с данными
- **Components** - управление UI
- **Utils** - вспомогательные функции
- **Config** - конфигурация и константы

Подробное описание архитектуры см. в файле [ARCHITECTURE.md](./ARCHITECTURE.md).

## Структура проекта

```
mpgu-stats/
├── src/                          # Исходный код приложения
│   ├── app.js                    # Главный класс приложения
│   ├── main.js                   # Точка входа
│   ├── config/
│   │   └── constants.js          # Константы приложения
│   ├── services/
│   │   ├── AuthService.js        # Сервис авторизации
│   │   ├── FirebaseService.js    # Сервис работы с Firebase
│   │   └── ExcelProcessor.js     # Обработка Excel файлов
│   ├── components/
│   │   └── UIController.js       # Контроллер UI
│   └── utils/
│       ├── validators.js         # Валидация данных
│       └── formatters.js         # Форматирование данных
├── index.html                     # Основной HTML файл
├── styles.css                     # Стили приложения
├── script.js                      # Старый код (для обратной совместимости)
├── README.md                      # Документация
├── ARCHITECTURE.md                # Описание архитектуры
├── REFACTORING_SUMMARY.md         # Резюме рефакторинга
├── render.yaml                    # Конфигурация для Render
├── firebase-rules.json            # Правила безопасности Firebase
└── FIREBASE_SETUP.md              # Инструкции по настройке Firebase
```

## Использование

### Загрузка данных

1. Выберите раздел (МПГУ, ЦИН, ЕГЛ)
2. Загрузите файл дневной статистики (Excel)
3. Загрузите файл часовой статистики (Excel)
4. Введите прогноз (24 числа через пробел)
5. Нажмите "Обработать файлы"

### Работа с таблицей

- **Экспорт в Excel**: Нажмите кнопку "📊 Экспорт в Excel" для сохранения таблицы
- **Копирование как изображение**: Нажмите кнопку "📷 Копировать как изображение" для копирования таблицы в буфер обмена
- **Удаление интервалов**: Нажмите "🗑️ Удалить интервалы" для удаления выбранных временных интервалов
- **Редактирование прогноза**: Нажмите значок ✏️ рядом с полем прогноза для редактирования

### Архив таблиц

- Просмотр сохраненных таблиц
- Экспорт архивных таблиц в Excel
- Удаление таблиц из архива

### Логи действий (для администраторов)

- Просмотр истории всех действий пользователей
- Поиск действий по email пользователя
- Понятные описания действий (например: "warapamm@gmail.com удалил интервал 18:00-19:00")
- Отображение даты, времени и деталей каждого действия

## Формат данных

### Структура Excel файлов

#### Дневная статистика
- Строка 1: Заголовки
- Строка 2: Данные
- Столбец A: Дата
- Столбец AB: AHT
- Столбец AI: Загрузка

#### Часовая статистика
- Строка 1: Заголовки
- Строки 2+: Данные (по одной строке на интервал)
- Столбец A: Дата
- Столбец B: Интервал (формат: "00:00 - 01:00" или "00:00")
- Столбцы I, O, V, AB, AC, AI: Метрики для каждого интервала

## Безопасность

- JWT авторизация через Firebase Authentication
- Защита от несанкционированного доступа к данным
- Валидация путей Firebase (предотвращение утечки данных)
- Санитизация данных для предотвращения XSS атак
- Content Security Policy (CSP)
- Логирование всех действий пользователей для аудита

## Разработка

### Модульная архитектура

Приложение использует модульную архитектуру с разделением ответственности:

#### Сервисы (Services)
- `AuthService` - работа с авторизацией
- `FirebaseService` - работа с Firebase Realtime Database
- `ExcelProcessor` - обработка Excel файлов

#### Компоненты (Components)
- `UIController` - управление UI элементами

#### Утилиты (Utils)
- `validators.js` - валидация данных
- `formatters.js` - форматирование данных

#### Конфигурация (Config)
- `constants.js` - константы приложения

### Добавление нового функционала

1. Создайте новый сервис в `src/services/`
2. Импортируйте его в `src/app.js`
3. Используйте через главный класс `App`

Пример:
```javascript
// src/services/NewService.js
class NewService {
    async doSomething() {
        // Логика
    }
}
export default new NewService();

// src/app.js
import NewService from './services/NewService.js';

// Использование
await NewService.doSomething();
```

## Логирование

Система автоматически логирует все действия пользователей:
- Обработка файлов (с указанием имен файлов)
- Обновление прогноза
- Экспорт данных (Excel, изображение)
- Удаление интервалов (с указанием удаленных интервалов)
- Работа с архивом (удаление, экспорт)
- Создание пользователей (только для администраторов)

Логи доступны в разделе "Логи действий" для администраторов и содержат:
- Email пользователя
- Дата и время действия
- Понятное описание действия
- Детали операции

Примеры описаний:
- "warapamm@gmail.com удалил интервал(ы) 18:00-19:00, 20:00-21:00"
- "user@example.com обработал файлы (часовой: stats.xlsx, дневной: daily.xlsx)"
- "admin@example.com создал пользователя newuser@example.com"

## Развертывание

Подробные инструкции по настройке Firebase и развертыванию см. в файле `FIREBASE_SETUP.md`.

### Требования

- Node.js (для локальной разработки)
- Firebase проект с настроенными Authentication и Realtime Database
- Render аккаунт (для хостинга)

### Локальная разработка

1. Клонируйте репозиторий
2. Настройте Firebase (см. `FIREBASE_SETUP.md`)
3. Откройте `index.html` в браузере или используйте локальный сервер:
   ```bash
   # Используя Python
   python -m http.server 8000
   
   # Используя Node.js
   npx http-server
   ```

## Преимущества новой архитектуры

- ✅ **Масштабируемость** - легко добавлять новые функции
- ✅ **Тестируемость** - каждый модуль можно тестировать отдельно
- ✅ **Поддерживаемость** - код организован и понятен
- ✅ **Переиспользование** - сервисы можно использовать в разных местах
- ✅ **Безопасность** - централизованная валидация и санитизация

## Лицензия

Проект разработан для внутреннего использования.
